{{#each model as |drink|}}
  <p>{{drink}}</p>
{{/each}}
{{#if canShowAddDrink}}
  <button class="btn btn-default" {{action "hideAddDrink"}}>Hide Form</button><
  <br><br>
  {{input value=newDrink enter="addDrink"}}
{{else}}
  <button class="btn btn-default" {{action "showAddDrink"}}>Show Form</button>
{{/if}}
{{outlet}}
